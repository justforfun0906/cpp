//code for fun
#include<bits/stdc++.h>
#define faster ios_base::sync_with_stdio(false);cin.tie(0);
#define pii pair<int, int>
#define vi vector<int>
#define ff first
#define ss second
typedef long long ll;
using namespace std;
int arr[100005];
int st[400005];
void build(int L, int R , int Q , int index){
	if(L==R){
		return;
	}
	else{
		int m=(L+R)/2;
		int lson=2*index;
		int rson=2*index+1;
		if
	}
}
int main(){
	int n;
	cin>>n;
	if(n!=0){
		memset(arr,0,sizeof(arr));
		memset(st,0,sizeof(arr));
	}
}

