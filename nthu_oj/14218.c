#include<stdio.h>
void solve(){
    int n,k; scanf("%d%d", &n, &k);
    int next[n+5];
    for(int i=1;i<=n;i++){
        next[i] = i+1;
    }
}
int main(){
    int t;
    scanf("%d", &t);
    while(t--){
        solve();
    }
}